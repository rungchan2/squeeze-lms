{
    "description": "Manages user roles and access control within the app.",
    "file_patterns": ["**/hooks/useRole.ts", "**/components/**/*.tsx"],
    "rules": [
      "Use Zustand to store user role and authentication state.",
      "Create a `useRole(requiredRole)` hook to restrict page access.",
      "Redirect unauthorized users to `/unauthorized`.",
      "Use conditional rendering for UI elements that depend on roles.",
      "Pages requiring admin privileges should use `useRole('admin')`."
    ]
  }