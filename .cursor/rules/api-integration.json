{
    "description": "Provides rules for API calls and data fetching with Supabase.",
    "file_patterns": ["**/lib/api/*.ts", "**/services/**/*.ts"],
    "rules": [
      "Use `supabase.from(table).select()` for fetching data.",
      "Always filter queries by `uid` when fetching user-specific data.",
      "Use `useSWR` or React Query (TanStack) for caching API requests.",
      "Handle errors gracefully using try-catch and Supabase error objects.",
      "Use Next.js API routes for server-side data fetching where needed."
    ]
  }