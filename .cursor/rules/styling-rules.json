{
    "description": "Defines styling preferences for Emotion and Chakra UI.",
    "filePatterns": ["**/components/**/*.tsx"],
    "rules": [
      {
        "name": "Emotion Usage",
        "description": "Use Emotion (`@emotion/react`) for all component styling instead of `styled-components`."
      },
      {
        "name": "Global Styles",
        "description": "Global styles should be defined in `styles/global.ts` using Emotion’s Global component."
      },
      {
        "name": "Chakra UI",
        "description": "Chakra UI should be used for layout-based components (Box, Flex, Grid)."
      }
    ]
  }